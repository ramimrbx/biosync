#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    if [ -d /run/systemd/system ]; then
        systemctl stop biosync.service 2>/dev/null || true
        systemctl disable biosync.service 2>/dev/null || true
        systemctl daemon-reload || true
    fi
fi
exit 0
