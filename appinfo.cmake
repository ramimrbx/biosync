# ─────────────────────────────────────────────────────────────────────────────
#  BioSync — SINGLE SOURCE OF TRUTH for app metadata
# ─────────────────────────────────────────────────────────────────────────────
#  Edit ONLY this file to change any of these. CMake reads it and feeds every
#  output automatically: the app binary + About panel, the Linux .deb, and the
#  Windows installer (NSIS + Inno Setup). You never edit the version or these
#  details in more than one place again.
#
#  APP_VERSION must stay in dotted numeric form (e.g. 1.2.0) — the update checker
#  compares it numerically, and the installers use it in filenames.

set(APP_NAME       "BioSync")
set(APP_PUBLISHER  "Right iTech")
set(APP_VERSION    "1.2.0")
set(APP_URL        "https://ritems.io")
set(SUPPORT_EMAIL  "info@ritems.io")
set(SUPPORT_PHONE  "+880 1XXX-XXXXXX")
